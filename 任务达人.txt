已完成：

- 主连发逻辑+进程分发

- 键盘自动重复机制（只影响热键钩子）

- worker名字

- 技能识别区域不用按enter，点保存按钮自动保存，技能识别按钮拉上来

- ex窗口布局整理
——————————————————————————————————————————————
未完成：
- 城镇识别(灰度)

- 脚本关闭和重新打开很慢

- 切换窗口不停止连发，托盘右键菜单去掉软件设置

- 子窗口行为（主窗口是否不可操作，返回主窗口时先解除不可操作，再hide子窗口）

- 连发性能（）

- Z键奔跑避让

- 修饰键防粘滞

- 截图窗口圆角

-  配置系统分层
从“能存”升级成了“有约束的预设管理”（AutoFireController）
V0 的 DNFAutoFire-0\core\Config.ahk 比较薄，只负责 ini 读写。
新版 core\Config.ahk 和 core\PresetManager.ahk 多了：
固定从脚本目录读 config.ini
自动初始化默认配置
预设顺序保存
预设重命名 / 克隆 / 删除 / 排序
0/1 布尔安全读取
主连发间隔、按下时长、单键覆盖间隔


9. 保留“同一时刻只跑一个连招序列”的现有 0.29 语义，避免行为突变

10.失焦和卡修饰键的处理方式换了
V0 靠 DNFAutoFire-0\core\ReleaseKeys.ahk 死循环补 Shift/Ctrl/Alt Up。
主线版改成 core\KeyRouter.ahk + core\GameContext.ahk，在游戏失焦时主动 FlushAllHeld，属于事件驱动，不再额外挂一个“放键修复线程”。

窗口检测更稳，兼容范围更大
V0 主要在 DNFAutoFire-0\core\Scripts.ahk 里 GroupAdd 少数几个标题。
新版 core\GameContext.ahk 同时兼容多个标题和多个 exe 名，还持续维护前台/存在状态缓存。

11. 单实例和进程生命周期管理明显加强
V0 是 #SingleInstance, Ignore。
新版用 core\SingleInstance.ahk 自己做 mutex 接管，重复启动时会尝试关闭旧实例再接棒；退出时 core\AppBootstrap.ahk 还会统一清理 mutex、GDI+、hotkey watcher、worker、timer resolution。


12. AutoFireController 在 core/AutoFireController.ahk (line 3)，作用是连发会话总控。

它负责这些事：

开始/停止整套连发：StartSession (line 43)、StopSession (line 68)

根据当前预设，把启用的主键启动成子进程 worker：MultipleThread.StartMainKeyThread(...) (line 55)

启动扩展功能模块，比如宠物技能、关羽、自动奔跑这些：StartFeatureModules (line 64)

切换预设、同步 UI 状态、改托盘图标：SwitchPreset (line 105)、UpdateTrayRunningIcon (line 182)

- 子进程路径最小化

- 主界面UI行为（手指）

- UI开关响应慢

- 快捷键输入框